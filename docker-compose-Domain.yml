# Domain Resilmesh Platform
include:
  - ./Aggregation/docker-compose-IT_Domain.yml
  - ./Security-Operations/docker-compose-IT_Domain.yml
  - ./Situation-Assessment/docker-compose-IT_Domain.yml
  - ./Threat-Awareness/docker-compose-IT_Domain.yml

services:
  resilmesh-redis:
    hostname: "redis"
    container_name: resilmesh-redis
    restart: unless-stopped
    image: valkey/valkey:7.2
    expose:
      - 6379
    networks:
      resilmesh_network:

networks:
  resilmesh_network:
    external: true
    name: resilmesh_network