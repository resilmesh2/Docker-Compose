# Domain Resilmesh Platform
include:
  - ./Aggregation/docker-compose-Domain.yml
  - ./Security-Operations/docker-compose-Domain.yml
  - ./Situation-Assessment/docker-compose-Domain.yml
  - ./Threat-Awareness/docker-compose-Domain.yml

services:
  resilmesh-redis:
    hostname: "redis"
    container_name: resilmesh-redis
    restart: unless-stopped
    image: valkey/valkey:7.2
    expose:
      - 6379
    networks:
      resilmesh_network:

networks:
  resilmesh_network:
    external: true
    name: resilmesh_network